import { AppDateFormatDirective } from './app-date-format.directive';

describe('AppDateFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDateFormatDirective();
    expect(directive).toBeTruthy();
  });
});
